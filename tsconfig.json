{
  "compilerOptions": {
    "allowImportingTsExtensions": true,
    "noEmit": true,
    "paths": {
      "@/*": [
        "./packages/*"
      ]
    },
    "types": [
      // 用来指定需要包含的模块，并将其包含在全局范围内。
      "element-plus/global",
      "./packages/components.d.ts",
    ]
  },
  "paths": {
    "@/*": [
      "packages/*"
    ],
  },
  "include": [
    "packages/**/*.ts",
    "packages/**/*.d.ts",
    "packages/**/*.tsx",
    "packages/**/*.vue"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ],
}